version: '3.7'

services:
  db_example:
    image: postgres:12-alpine
    environment:
      POSTGRES_PASSWORD: 1234
      POSTGRES_USER: app
      POSTGRES_DB: app
    ports:
      - '5435:5432'
    volumes:
      - ./init_db:/init_db
